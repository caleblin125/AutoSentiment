# Core API
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0

# Settings & HTTP
pydantic-settings>=2.6.0,<3.0.0
httpx>=0.27.0,<1.0.0

# Persistence
sqlalchemy>=2.0.36,<3.0.0
aiosqlite>=0.20.0,<1.0.0

# HTML → text (pick one primary path in ingest)
trafilatura>=1.12.0,<2.0.0

# Optional: LLM SDKs (uncomment what you use)
# openai>=1.0.0
# anthropic>=0.40.0
